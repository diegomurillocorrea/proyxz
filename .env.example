# Copiá este archivo a .env.local y completá los valores (no commitees .env.local).

NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave publicable nueva (dashboard Supabase) o clave anónima JWT (anon key):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# Base de datos: ejecutá el SQL en supabase/migrations/20260413000000_proyxz_schema.sql
# (Supabase → SQL Editor → New query → pegar → Run).
